@model IEnumerable<LabASP.Models.Product>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Наименование_)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Фотография)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Стоимость)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status.Статус)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Наименование_)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Фотография)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Стоимость)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status.Статус)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Код_товара }) |
            @Html.ActionLink("Details", "Details", new { id=item.Код_товара }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Код_товара })
        </td>
    </tr>
}

</table>
